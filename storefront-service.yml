server:
    port: 8080

spring:
    application:
        name: storefront-service
    cloud:
        config:
            uri: http://localhost:8888/
        consul:
            host: localhost
            discovery:
                port: 8080
                instance-id: ${spring.application.name}:1
                service-name: storefront-service


my:
    test:
        value: Refreshed hello from server local


